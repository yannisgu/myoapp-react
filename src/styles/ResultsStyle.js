import base from './BaseStyle'

let style = {
    listViewRow: {
        ...base.listViewRow
    },
    listViewRowText: {
        ...base.listViewRowText,
        fontSize: 20
    },
    listViewCell: {
        ...base.listViewCell

    }
}

export default style;
